깃허브 push/ pull(올리기, 내리기)
push : 권한o
pull : 권한X
PATC(personal Access Token)로 연결
쓰기 권한이 있어야 push가 가능
-자신의 저장소나 다른 사람의 저장소라면 협업자로 등록
push
-로컬 저장소에서 남겨놓은 코드 변경 이력들이 원격 저장소로 전송
명령
-$git push<저장소 별칭명><브랜치명>
-$git push origin topic

옵션 -u : 모든 인자를 생략 가능
git pull = git fetch + git merge
git fetch : 원격 저장소의 정보를 로컬(remote/origin)저장소로 가져오는 명령
git merge : 변경된 정보를 로컬 저장소의 내용과 병합

fetch 명려어
-$git fetch<remote>
-$git fetch origin

내용 반영하려면 fetch 후 merge 해야함
